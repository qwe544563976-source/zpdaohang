# V2 升级包 · 安装与逐句对账说明

日期：2026-08-20
性质：拆网定案 Q6"中央一次合成、单次交付"的交付说明。生产窗口只装包、只生产，不参与代码改造讨论。
升级包位置：本仓库 `book-to-judgment-navigation/` 目录。

---

## 老板版

一句话：**升级包把 9 个 cangjie 文件和知识地图并进一条流水线，装上步骤级哈希、方案 C、留插座三件套；装包五步，先备份、再覆盖、跑测、回填、恢复生产。**

---

## 一、安装步骤（你本机执行，按顺序）

1. **备份**：把 `C:/Users/aa/.codex/skills/` 下的 `book-to-judgment-navigation`、`cangjie-skill`、`book-to-skill` 三个目录打一个带时间戳的 zip，放到 `.codex/skills/` 之外。
2. **装新包**：删除旧 `book-to-judgment-navigation/`，用本仓库的 `book-to-judgment-navigation/` 整目录替换。
3. **恢复原厂**：删除现有 `cangjie-skill/`、`book-to-skill/`，分别用本仓库根的 `cangjie-skill-main.zip`、`book-to-skill-master.zip` 解包替换（这两个 zip 就是 GitHub 原厂版）。原来装在里面的 9 个项目文件已全部搬进新包，不会丢东西（见对账表 A）。
4. **跑测**（全部在 `book-to-judgment-navigation/` 下）：

   ```bash
   set PYTHONUTF8=1
   python -m pytest scripts/test_build_methods.py scripts/test_advance_batch_state.py scripts/test_merge_method_batch.py scripts/test_adapt_jh8_facts.py scripts/test_validate_delivery.py scripts/test_backfill_step_hash.py
   ```

   预期：148 项全过（你机器上 JH8 样例包和历史 ch11 夹具都存在，跳过项会变成实跑）。少于基线 127 即停止装包并报告。
5. **回填 635 条**：

   ```bash
   python scripts/backfill_step_hash.py <全书总账的 method-recipes.json> --check   # 先看报告
   python scripts/backfill_step_hash.py <全书总账的 method-recipes.json>           # 确认后写入（自动备份）
   ```

   纯计算：补 `step_hash`、补 `spine_slot_hint: null`、生成器身份改新地址；不动任何经文内容。
6. **物理清障**（定案步骤 1，只能你本机做）：`bphs-97-book/` 移入 `_已退役/`；`_backups/` 移出 `.codex/skills/`。
7. **恢复生产**：第一批 BPHS 14~16 章零件（Q20），开工只读新 SKILL.md（必读卡）。

**过渡注意**：旧格式（v2 整包绑定）的独立审计报告从此被合入脚本拒绝。如有已完成审计、尚未合并的旧批次，按规矩新建目录重产——步骤指纹未变的 PASS 凭证条目可按 `step_hash` 原样搬入新的 v3 报告，只重审被改步骤。

## 二、对账表 A：文件搬家（旧位置 → 新位置）

| # | 旧位置 | 新位置 | 变化 |
|---|---|---|---|
| 1 | cangjie-skill/scripts/build_methods.py | book-to-judgment-navigation/scripts/build_methods.py | 改造，见对账表 C |
| 2 | cangjie-skill/scripts/test_build_methods.py | scripts/test_build_methods.py | 适配＋新增 8 项测试 |
| 3 | cangjie-skill/scripts/prepare_method_source.py | scripts/prepare_method_source.py | 原样 |
| 4 | cangjie-skill/references/generation-rules.md | references/generation-rules.md | 入口路径更新＋新增"v2 升级新增的机器拦截"节 |
| 5 | cangjie-skill/references/method-extraction-prompt.md | references/method-extraction-prompt.md | 原样 |
| 6 | cangjie-skill/references/method-extraction.schema.json | references/method-extraction.schema.json | method 增加必填 `spine_slot_hint`（只许 null） |
| 7 | cangjie-skill/references/terra-audit-prompt.md | references/terra-audit-prompt.md | 改写为 v3 凭证＋审计三件事＋高风险词单选确认 |
| 8 | cangjie-skill/templates/JUDGMENT_METHOD.json.template | templates/JUDGMENT_METHOD.json.template | 步骤加 `step_hash`（机器填）、方法加 `spine_slot_hint: null` |
| 9 | cangjie-skill/methodology/08-judgment-navigation-mode.md | methodology/08-judgment-navigation-mode.md | 加"新家说明"头，正文原样 |
| 10 | book-to-skill/SKILL.md 第 5 节（Mode 5 知识地图，63~124 行） | references/knowledge-map.md | 整段搬入为流水线第 2 步；仅两处改动：出处标注、入口路径 |
| 11 | book-to-judgment-navigation/SKILL.md（21KB 详规） | SKILL.md（约 3 千字必读卡）＋ references/full-manual.md（备查字典，正文原样＋降级说明头） | 拆网定案步骤 2 流程降维 |
| 12 | cangjie-skill SKILL.md 的"判断方法蒸馏模式""生产不空转纪律"两节 | 内容已分别存在于 methodology/08、references/generation-rules.md 和 references/full-manual.md，无独有丢失 | cangjie-skill 恢复原厂 |
| 13 | cangjie-skill、book-to-skill 其余全部 | 恢复原厂（仓库两个 zip） | 项目专用内容清零 |

## 三、对账表 B：新增文件

| 文件 | 用途 | 定案依据 |
|---|---|---|
| scripts/shared_constants.py | 两组黑名单只搬不改（实测逐字一致后抽出）＋生成器身份＋step_hash 算法＋四类高风险词词表与检测 | 拆网四·1、Q1、Q15 |
| scripts/backfill_step_hash.py ＋ test_backfill_step_hash.py | 一次性回填已并入 635 条的 step_hash，幂等、带备份、--check 只读 | 拆网四·5 |
| tests/make_pilot_fixture.py | 现场自举验证器回归夹具，让 64 项验证器测试脱离你本机的 ch11 历史夹具也能跑；不迁移、不冒充 v7 内容 | Q6"跑通全部单测" |
| references/knowledge-map.md | 知识地图第 2 步说明（搬家产物） | Q19 |
| references/full-manual.md | v1 详规备查字典（降级产物） | 拆网步骤 2 |

## 四、对账表 C：代码改动逐处对账

| # | 位置 | 改了什么 | 定案条目 |
|---|---|---|---|
| 1 | build_methods.py 头部（原 28~42 行两组黑名单） | 删除本地定义，改 import shared_constants；实测两份逐字一致后只搬不改 | 拆网四·1 |
| 2 | build_methods.assemble_step | 每步计算并写入 `step_hash`（对除 step_hash 外全部字段做键排序紧凑 JSON 再 sha256） | Q1 |
| 3 | build_methods.validate_claim | 新增：逐字短引中检出的四类高风险词必须被 claim_terms 映射覆盖，未映射直接拒绝；返回差异表行（词、类别、映射引文词、映射主张词） | Q15 |
| 4 | build_methods.assemble | 差异表写入 `generation.high_risk_terms` 和生成报告；方法输出复制 `spine_slot_hint`（null） | Q21、Q17 |
| 5 | shared_constants.GENERATOR_ID | "cangjie-skill/scripts/build_methods.py" → "book-to-judgment-navigation/scripts/build_methods.py"；同一生成器换地址，不设双身份 | Q19 |
| 6 | validate_delivery.py 头部（原 112~126 行两组黑名单） | 同 #1，改 import | 拆网四·1 |
| 7 | validate_delivery.validate_generation_metadata（原 1388 行） | 生成器身份改用共享常量；新增调用 #8 | Q19 |
| 8 | validate_delivery.validate_step_hashes_and_high_risk_terms（新函数） | v2 配方逐步重算 step_hash 比对（手改即败）；重放高风险词检测并与差异表比对；映射词必须真在短引和最小意思中 | Q1、Q15、Q3b |
| 9 | validate_delivery.load_receipt / validate_status_receipts（原 1937~2049 行） | v2 配方（步骤带 step_hash）的核验凭证：必填 `method_step_hashes` 逐步骤绑定，整包 `method_recipes_sha256` 绑定退出必填；v1 历史夹具保持原行为（版本判定，非兼容双读） | Q1 |
| 10 | merge_method_batch.verify_audit（原 165~202 行） | 审计报告升 `independent-semantic-audit/v3`：整包绑定退役；每个 entry 必须携带与当前配方一致的 `step_hash`；`high_risk_confirmations` 必须与机器差异表清单逐词一致、只许 accept/reject、有 reject 必须判 REJECT | Q1、Q13、Q21 |
| 11 | advance_batch_state.py | **无改动**。定案表只点名 2 个脚本，实测整包哈希在 4 个脚本使用；其中 advance:150 与 merge:338 的批次状态绑定属于"同目录内防手改"，与死循环无关，保留原样 | 对定案四·2 的颗粒度修正 |
| 12 | test_validate_delivery.py:21、test_adapt_jh8_facts.py:9 | 写死的 C:/、F:/ 路径改为：环境变量（JUDGMENT_NAV_PILOT_FIXTURE / JUDGMENT_NAV_JH8_CHART）→ 本机历史路径（你机器行为不变）→ 夹具自举（仅验证器测试）；JH8 样例包缺失时该 1 项显式跳过，不假绿 | Q6 |
| 13 | method-extraction.schema.json ＋ 模板 | `spine_slot_hint` 必填且只许 null；现在一律填 null，不猜 PVR | Q17 |

## 五、对账表 D：单测

基线（拆网定案）：127 项 = build 24 ＋ advance 7 ＋ merge 15 ＋ jh8 17 ＋ validate 64。

升级后：**148 项** = build 32（＋8：step_hash 3、spine_slot_hint 2、高风险词 3）＋ advance 7（不变）＋ merge 19（＋4：v2 退役、指纹失配、确认缺失、reject 强制）＋ jh8 17（1 项在没有 F 盘样例包的机器上显式跳过）＋ validate 68（＋4：指纹复核、差异表复核、v2 凭证逐步绑定、v1 保持整包绑定）＋ backfill 5（新）。

本仓库实测：147 过、1 跳过（JH8 样例包在你本机，装包后 148 全跑）。任何一项失败都不得装包。

## 六、遗留与边界（不冒充已完成）

- 回填 635 条、物理清障、恢复生产在你本机执行（第一节第 5~7 步）。
- 12 题盲测卷发 RAG 窗口（定案步骤 3）不归本升级包管。
- Q13b/Q16 的隔离（quarantined_candidate）当前按流程执行（必读卡＋审计提示词已写死规则）；机器强制等第一个真实隔离案例出现后按"真实失败证据"纪律再加闸，不提前造电器。
- PVR、K.N. Rao、300C 未加工；`spine_slot_hint` 全部 null。
- 本仓库没有正式书包（accepted-package/evidence_atoms.jsonl 在你 F 盘）；蒸馏生产开工的输入问题见主对话。
