# PIPELINE_STATE

## 工作单

- 工作单：`WORK_ORDER.json`
- 当前操作：PILOT_TEMP（临时测试夹具） / FORMAL（正式书包重跑）
- 当前阶段：输入核对
- 最近更新时间：YYYY-MM-DD HH:MM
- 唯一下一步：填写一项可直接执行的动作

## 状态隔离

- `pilot_only（仅供试验）`：true / false
- `publishable（允许正式发布）`：false
- `artifact_scope（产物身份）`：PILOT_TEMP / FORMAL
- 当前输入：试验夹具工作单 / accepted-package.json（已验收书包凭证）
- 当前原文内容摘要：
- 允许章节或原文编号范围：

## 阶段对账

- [ ] 开工检查：写入责任、正式书包实时检查、逐页结构反查凭证、范围和只读边界已核对
- [ ] 清点原料：章节／主题／术语知识地图与全书原文分类总账完成，编号集合对齐，待审为 0
- [ ] 校准模具：10 个跨章节高风险样品完成逐步骤证据、条件分支、停止条件、机器检查、三对照和独立语义检查
- [ ] 分批生产：每个专题批次完成方法配方、来源去向更新、四路查询、派生文件重生和本批闭环
- [ ] 联机验收：JH8（排盘数据）和 RAG（查原文系统）分别接通；实际问题考试、用户批准和书籍岗位开放完成

## 专题批次对账

| 专题和本批范围 | 输入原文数量 | 新增方法／步骤 | 机器检查 | 正确／错误／空白 | 独立语义检查 | 当前状态与唯一下一步 |
|---|---:|---:|---|---|---|---|
| 尚未开始 | 0 | 0／0 | 未做 | 未做 | 未做 | 等开工检查通过 |

## 已完成产物

- 暂无。

## 验收记录

- 尚未运行。

## 上游问题

- 机器记录见 `UPSTREAM_ISSUES.json`，人读展示见自动生成的 `UPSTREAM_ISSUES.md`。

## 未解决问题

- 暂无。

## 防失忆规则

- 下一次开工先读本文件，再核对真实产物。
- 每个正式批次开工前，用 `tools.register_accepted_package.run_authoritative_validation`只检查工作单绑定的当前书包；上一批通过不能替下一批免检，另一册失败也不能连坐本书。
- 本文件只记录阶段、数量、路径、失败和下一步。
- 某阶段标为完成但产物不存在时，改为“状态漂移”，只修该阶段。
- 试验产物不得切换为正式发布；正式已验收书包到位后全部重跑，不迁移编号、方法或核验结论。
