interface:
  display_name: "原文原子判断导航"
  short_description: "从已验收书包或隔离试验夹具生成可核验判断导航"
  default_prompt: "Use $book-to-judgment-navigation with this accepted-package.json or explicit PILOT_TEMP fixture, use evidence_atom_id as the only source anchor, treat PILOT_STATUS as a hard deny authority that no receipt can override, and require real positive, known-bad, and blank command runs before reporting a mechanical gate pass."
