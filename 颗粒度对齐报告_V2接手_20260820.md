# 颗粒度对齐报告 · V2 升级接手

日期：2026-08-20
性质：接手窗口对齐报告。只对齐、不改工具；改造等本报告的 4 个拍板点确认后才开工。
核查方式：仓库里所有文件实际解包、实际读码、实际跑单测；下文每个数字都有出处，没有估算。

---

## 老板版

一句话：**《拆网定案》是唯一执行版本，两份规划书降级为原则背景；定案指名的改造点我在代码里逐一找到了，但有 4 处颗粒度出入需要你拍板——最大的一处是"步骤级哈希"实际要动 4 个脚本而不是定案写的 2 个，其次是 127 项单测里 65 项在这个仓库因两条写死的本机路径跑不起来。**

---

## 开工路径图

```text
┌─────────────────────────────────────────────────────────┐
│ 🟢 已完成：解包三个 zip，读完定案、状态总账、合同、      │
│    三个工具的 SKILL 和全部脚本，实测单测基线             │
└──────────────────────────┬──────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 🔵 当前：本报告 = 颗粒度对齐，列出 4 个拍板点            │
└──────────────────────────┬──────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 🟡 待做：你确认后，我在本分支做 Q6 中央一次合成          │
│    （5 处代码改造＋必读卡＋回填脚本＋对账表＝唯一升级包）│
└──────────────────────────┬──────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 🔴 只能在你本机做：装包、回填 635 条、物理清障、         │
│    发 12 题盲测卷、恢复 14~16 章生产                     │
└─────────────────────────────────────────────────────────┘
```

---

## 一、我实际核查到的现状（全部有据）

### 1. 仓库里有什么

| 文件 | 是什么 | 核查结果 |
|---|---|---|
| `拆网定案_v2升级_20260820 (1).md` | V2 唯一执行版本 | 22 条定案＋拆网四步＋5 处代码改造清单 |
| `蒸馏导航相关原版完整备份_20260820T144128.zip` | 改造前的原版工具 | 含 4 个 skill、状态总账、写入所有权表、反假绿工具、全局 AGENTS.md；备份说明写明这是"安装必读卡之前"的原版 |
| `book-to-skill-master.zip`、`cangjie-skill-main.zip` | GitHub 原厂版 | 与备份版逐目录 diff 过：备份版 = 原厂 ＋ 项目专用文件（见下） |
| `book.md`（690,544 字节）、`pages.jsonl`（224 页） | BPHS97 的 OCR 干净正文和逐页记录 | 上游产物，见第四节边界说明 |

### 2. 状态总账（2026-08-19）关键数字，全部对上

- 当前阶段：`FORMAL_DISTILLATION_V2_FULL_BOOK_STAGE_A`（正式蒸馏、全书 A 阶段）
- BPHS97 正式书包已登记：2024 条原文原子；剩余待生产候选 1389 条 → 已并入 2024 − 1389 = **635 条**，与定案 Q1 写的"已并入 635 条"闭合
- UK（《乌塔拉·卡拉姆里塔》）：P0 停机（第 4、5、14 页 OCR 污染），不连坐 BPHS
- JH8 排盘事实字段、RAG 正式查询接口：均未交付（阻塞执行，不阻塞方法生产）

### 3. 单测基线实测：127 项，与定案一致

| 套件 | 数量 | 在本仓库跑的结果 |
|---|---|---|
| build_methods | 24 | 🟢 全过 |
| advance_batch_state | 7 | 🟢 全过 |
| merge_method_batch | 15 | 🟢 全过 |
| adapt_jh8_facts | 17 | 🟡 16 过，1 挂（`test_adapt_jh8_facts.py:9` 写死 `F:/JH8-.../LML_true_lahiri`） |
| validate_delivery | 64 | 🔴 全挂（`test_validate_delivery.py:21` 写死 `C:/Users/aa/.../_judgment_distillation_pilot_bphs97_ch11_20260814` 试点夹具，本仓库没有这个目录，整个测试类 setUp 就死） |

合计 24＋7＋15＋17＋64 = 127，与定案"127 项单测基线"完全一致。差别在于：定案说"两处失败只因 Windows 路径，在用户机器上会过"——在你机器上对，但在这个仓库里试点夹具整个目录缺失，等于 **65 项跑不了**。这直接影响 Q6"跑通全部单测再交付"，见拍板点 2。

### 4. 定案指名的改造点，逐一在代码里找到了

| 定案条目 | 代码现状（备份原版） |
|---|---|
| Q1 步骤级哈希 | `step_hash` 不存在 ✓（待新增）；整包哈希 `method_recipes_sha256` 存在，但在 **4 个脚本**里被写入/校验，不止定案表里的 2 个——见拍板点 1 |
| Q15 方案 C | `validate_claim()` 在 `build_methods.py:208`，`claim_terms` 已在 schema 第 148 行；四类高风险词强制映射待加 |
| Q17 留插座 | `spine_slot_hint` 在模板和 schema 中均不存在 ✓（待加） |
| 拆网步骤 1 共享常量 | `shared_constants.py` 在备份里**不存在**；`EMPTY_CLAIM_FRAGMENTS`／`PLACEHOLDER_FACT_PATTERNS` 仍重复出现在 `build_methods.py` 和 `validate_delivery.py` 两处（已验证）。定案写"已完成"指的是你本机沙箱，本仓库要重做 |
| Q19 知识地图搬家 | book-to-skill 备份版 SKILL.md 第 5 节 `Accepted Evidence Knowledge Map`（已验收证据知识地图），纯流程文字、不依赖 book-to-skill 的提取代码，可以整段搬进 book-to-judgment-navigation 当第 2 步 ✓ |
| 交付清单"9 个 cangjie 文件" | 逐一点名对上：`scripts/build_methods.py`、`scripts/test_build_methods.py`、`scripts/prepare_method_source.py`、`references/generation-rules.md`、`references/method-extraction-prompt.md`、`references/method-extraction.schema.json`、`references/terra-audit-prompt.md`、`templates/JUDGMENT_METHOD.json.template`、`methodology/08-judgment-navigation-mode.md`，正好 9 个；搬走后 cangjie-skill 与原厂 zip 对齐 ✓ |

---

## 二、三份文件的关系：颗粒度裁定

**裁定：《拆网定案》（08-20）> 状态总账（08-19）> 两份规划书（08-14 前后）。冲突处一律按定案执行，两份规划书保留原则层价值，执行细节不再作数。**

两份规划书与现实的具体出入：

| 规划书写的 | 现实 | 裁定 |
|---|---|---|
| "尚未实施"、v7 只做第 11 章小样、正式重切后从零重跑 | 正式重切已完成（08-18 书包登记），已从零生产并入 635 条 | 规划书的"v7 临时试验"章节全部成为历史，不再指导执行 |
| 第一批先做 4 类方法骨架（完整命盘／行星／宫位／运期） | 定案已裁决：跨章拼骨架违反 PVR 铁律，**认错留档，不得再提**；BPHS 只出零件＋单章作者明写清单（6 处已实地取证） | 按定案。第一批 = 14~16 章零件，第二批 = ch74 决策树 |
| 三档考卷（初级／中级／高级，需 JH8＋RAG＋命盘底账） | JH8 字段、RAG 接口均未交付 | 三档考卷继续等接口；现在能跑的考试 = 12 题盲测卷发 RAG 窗口（定案步骤 3）＋每批正常／错误／空白三对照 |
| 状态流"候选→机械→独立→Shadow→用户批准→已发布" | 已实现为批次状态机 `未开工→生成中→机检仿真→审计中→已并入`；用户审批收敛为 3 个里程碑（首次进抽审、全书完成、正式发布），不逐批签字 | 按已实现的状态机，规划书命名不再使用 |
| "8 项／6 项／10~20 次检索不能写死" | schema 已落地：`retrieval_plan` 四路查询（支持／反例／边界／方法）、`stop_conditions`、逐步骤证据三件套、5 档来源状态全部存在（schema 第 298~324 行验证过） | 规划书这部分原则已经变成代码，无需再执行 |

一句话：**两份规划书当年要建的东西，绝大部分已经建成并写进了 schema 和验证器；它们剩下的价值是"为什么这样设计"的原则记录，不是待办清单。**

---

## 三、四个拍板点（对齐的核心产出）

### 拍板点 1：Q1 步骤级哈希的真实波及面是 4 个脚本，不是定案表里的 2 个

整包哈希 `method_recipes_sha256` 的实际使用位置（已逐行核实）：

```text
build_methods.py:573          写入批次状态
validate_delivery.py:1400     校验批次状态与文件一致
validate_delivery.py:1965     校验审计凭证绑定（validate_status_receipts）
advance_batch_state.py:150    送审前校验
merge_method_batch.py:171,338 合入前校验
```

定案表只列了 `build_methods.assemble_step` 和 `validate_delivery.validate_status_receipts`。只改这两处，advance 和 merge 的整包哈希校验仍会在"改第 7 步重新生成文件"后把全部凭证判废——死循环没切干净。

**建议方案**：整包哈希保留，但职责降级为"文件自生成后没有被手改"；审计凭证一律改绑 `step_hash`；advance／merge 校验凭证时按步骤对哈希，未变步骤的凭证继续有效。改动仍然是纯机械对账，不动任何经文内容。

### 拍板点 2：单测基线在本仓库缺 65 项，需要补夹具或改测试路径

Q6 要求"跑通全部单测"再交付升级包。本仓库缺两样：

1. 试点夹具目录 `_judgment_distillation_pilot_bphs97_ch11_20260814`（validate 64 项全部依赖）
2. JH8 样例命盘包 `01_命盘完整包.json`（jh8 1 项依赖）

**建议方案（两条并行）**：
- 我把两个测试文件里写死的路径改成"环境变量可覆盖、默认值不变"——只动测试文件，你机器上行为一字不变；
- 你把试点夹具目录和一份 JH8 样例包打 zip 传上来，我在这里把 127 项全部跑绿再交付。传不了的话，65 项以"你本机装包后跑"为准，我在交付说明里写明这个缺口，不冒充全绿。

### 拍板点 3：《必读卡》仓库里没有现稿，由我按定案起草

备份说明写明备份先于"安装必读卡"，所以你机器上已装的必读卡不在我手里。**建议**：我按定案＋合同重新起草约 3 千字符的必读卡（作为新 SKILL.md），你终审；如果你机器上已有满意的现稿，把它传上来我以它为准。两份内容以后不许并存，装包时覆盖。

### 拍板点 4：四件事只能在你本机做，我只交付脚本和说明

1. 回填 635 条已并入方法的 `step_hash`（我交 `backfill_step_hash.py`＋使用说明，总账在你机器上）
2. `bphs-97-book`（9MB、217 个文件，已实测）移入 `_已退役/`；`_backups` 移出 `.codex/skills/`
3. 12 题盲测卷发给 RAG 窗口开考（卷子不在仓库里）
4. 装升级包后在你机器跑 127 项单测复核

---

## 四、这个仓库能做什么、不能做什么（边界，先说清防误会）

**能做**：Q6 中央一次合成的全部内容——5 处代码改造、必读卡、知识地图搬家、cangjie／book-to-skill 恢复原厂说明、回填脚本、逐句对账表，打成唯一升级包。

**不能做，也不该做**：用 `book.md`／`pages.jsonl` 直接蒸馏。这两个文件是上游 OCR 产物；按你们自己的合同（SKILL"输入边界"第一条：不读取 PDF、book.md 或 pages.jsonl 补证据），蒸馏只认已验收书包里的冻结原文原子。正式书包（`accepted-package.json`＋`evidence_atoms.jsonl`，2024 条）在你的 F 盘，不在仓库。所以**本仓库升级工具没问题，恢复批次生产必须回你本机**。如果你的意思是让我在这里从 book.md 重新切原子——那是切卡窗口的活，会造出第二套编号体系，违反"不映射、不混写"的定案，我不做，也提醒你别让任何窗口做。

---

## 五、对齐后的执行顺序（等你拍板后开工）

1. 你回复四个拍板点（同意／改），顺手决定是否上传试点夹具、JH8 样例包、必读卡现稿
2. 我在本分支完成中央合成，顺序：`shared_constants.py` 抽公共黑名单 → step_hash 三件套（含 4 脚本凭证改绑）→ 方案 C 四类高风险词 → `spine_slot_hint` 插座 → `backfill_step_hash.py` → 知识地图搬进流水线第 2 步 → 必读卡 → 逐句对账表；每步跑单测，不少于基线
3. 你本机：装包 → 跑 127 项 → 回填 635 条 → 物理清障 → 发盲测卷
4. 恢复生产：14~16 章零件第一批 → ch74 决策树第二批 → 三条判据（连续 3 批零新病根、100% 一轮通关、总账闭合）达标 → 开并行线

---

## 收尾路径图

```text
┌────────────────────────────────────────────────────┐
│ 🟢 对齐完成：现状核实、三份文件裁定、4 个拍板点     │
│    （本报告已入库，分支 claude/v2-upgrade-…）      │
└──────────────────────────┬─────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────┐
│ 🟡 等你：4 个拍板点回复＋可选上传（夹具/JH8包/必读卡）│
└──────────────────────────┬─────────────────────────┘
                           ↓
┌────────────────────────────────────────────────────┐
│ 🔴 未开工：Q6 中央合成升级包（拍板后本分支执行）    │
└────────────────────────────────────────────────────┘
```
