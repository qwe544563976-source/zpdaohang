# 拆网与 v2 升级 · 定案

日期：2026-08-20
性质：经四轮拷问逐条拍板，**本文是唯一执行版本**。上下文丢失后只读这一份即可继续。
状态：设计已封版，等待执行。生产线当前全停。

---

## 老板版

一句话：**BPHS 是第二层法典（只出规则零件），PVR 才是第一层脊梁（出看盘顺序）；死循环靠"步骤级哈希 ＋ 单步否决 ＋ 一轮封顶"三件套切断；技能拆成一条从头到尾的流水线，开工只读一张 3 千字的卡。**

---

## 一、岗位定位（这一条管住所有越权）

```text
第 1 层【交警·流程脊梁】 = PVR（515 页整盘步骤教材）   ← 尚未加工，位置留空
第 2 层【法典·规则零件】 = BPHS 97 ＋ UK ＋ 300C        ← BPHS 正在做
第 3 层【闹钟·时间判断】 = K.N. Rao                     ← 尚未加工，位置留空
```

**铁律（来自 `contracts.md` 11.4，本次重申）**：PVR 未正式加工前，**完整看盘顺序保持不可用**；K.N. Rao 未正式加工前，完整时间判断保持不可用。

### 由此推出的分水岭（Q14 定案）

| 做法 | 判定 |
|---|---|
| 跨章拼接出 BPHS 没写过的整盘顺序（例：跨 ch9＋ch25＋ch48 拼全盘底审） | ❌ **严禁**，这是 PVR 的活 |
| 提取单章内作者自己写明的检查清单 | 🟢 **合规**，按正常零件规格提取，逐字锚定，**不标 synthesized_candidate** |

已确认属于"单章内作者明写"的位置（子代理实地取证，可直接开工）：

| 出处 | 原文逐字短引 | 是什么 |
|---|---|---|
| ch74.11-14 | `The effects of a bhava will be in accordance with the grah occupying it or in accordance with the grah aspected if no grah is in occupation of the bhava.` | 宫位判定决策树（全书最接近决策树的一段） |
| ch28.15-20 | `The actual effects will be a combination of bhava strength and its lord's strength.` | 宫力＋宫主力加减清单 |
| ch11.14-16 | （宫兴衰核对项） | 宫的兴衰检查项 |
| ch47.2-6 | `two kinds of effects of Dashas: general and distinctive` | 运期判断框架 |
| ch48.5-8 | `the placement of a grah at the time of birth and at the commencement of the Dasha, should both be taken into account` | 运期必核两项 |
| ch25.86-87 | `should be wisely conceived by their positions, relations, and drishtis apart from their strength or weakness` | 看一颗行星四要素（并列，非顺序） |

> **主窗口认错留档**：我曾提议"暂停零件、在 BPHS 里拼 4 张全盘骨架"（原 Q4/Q5）。该提议**违反上面那条铁律，已被裁决推翻，不得再提**。骨架属于 PVR，BPHS 只出零件和单章清单。

---

## 二、二十二条定案

### A 组：切断死循环（三件套，必须一起上）

| 编号 | 定案 |
|---|---|
| **Q1** | **步骤级哈希**：每个 step 独立算内容指纹 `step_hash`；审计凭证逐步骤绑定。改第 7 步只让第 7 步失效，其余步骤"已通过"保持有效。已并入 635 条写脚本一次性回填，**纯计算、不动任何经文内容**。 |
| **Q13** | **放行公式**：`机器检查 Exit 0 ＋ 审计确认无私货 ＝ 这一步放行`。机器全过 ≠ 直接放行（撤销"熔断闸 1"）。审计**保留否决权，但作用域锁死单步**。 |
| **Q13b** | **一轮封顶**：审计抓出某步有私货 → 只打回该步重写 **1 次**；改完仍有争议 → 该步直接进 `quarantined_candidate` 隔离，**绝不给第二次挑刺机会**。 |
| **Q16** | **凭证按步骤走，并入按方法走**：任一步骤被隔离 → **整个方法**不并入（不许残缺方法进总账）；同批其他健康方法**照常并入**。 |

**为什么这三件套能终止循环**：旧设计 `method_recipes_sha = sha256(整份文件)`，改一个错别字 → 整批 100 步凭证全废 → 全批重审 → 新一轮又挑出别的 → 数学上永不收敛。步骤级哈希切断强耦合，一轮封顶给出硬出口。

### B 组：审计的笼子

| 编号 | 定案 |
|---|---|
| **Q3** | 审计**只准做三件事**：①勾选已登记错误家族 ②判"这一步有没有原文支持不了的私货" ③附栏报疑似新家族，**最多 2 条、必须带原文与复现路径、不阻塞本批合并**。 |
| **Q3b** | 机器已经查过的**严禁重查**：格式、字段、逐字引文子串、页码、五种分支通电、占位事实、空话复读、状态跳级。 |
| **Q21** | 机器扫出的**高风险词差异表给审计员看**，但戴三道手铐：**①输出格式锁死为单选 JSON（只有 accept / reject）②只准针对机器列出的那 1~2 个词做确认，严禁在机器清单外自己找茬 ③一轮封顶**。审计员从"土皇帝"降为"按清单打勾的盖章员"。 |

### C 组：私货检测

| 编号 | 定案 |
|---|---|
| **Q15** | **采用方案 C 中间档**。否决全量词映射表（token 暴增、长 JSON 易截断）；否决纯靠人审（漏"角分写成弧度""童年无来源"）。**只强制映射四类高风险词：专有名词（行星／宫位名）、数值、单位、时间词**——一步通常只有 2~5 个，token 几乎零增量。 |

### D 组：留插座

| 编号 | 定案 |
|---|---|
| **Q17** | 在 `JUDGMENT_METHOD.json.template` 和 schema 里加一行 `spine_slot_hint: null`。**现在一律填 null，不猜 PVR 长什么样**；将来 PVR 进场直接填值，不必回头重构已并入的 JSON。留插座，不造电器。 |

### E 组：拆网形态

| 编号 | 定案 |
|---|---|
| **Q18** | **目录名不改**，仍叫 `book-to-judgment-navigation`；内部文档统一标注 **v2 升级版**。改名会让几十处导入路径和已并入记录里的引用崩盘——改名属身份变更，不混进搬家。 |
| **Q19** | **知识地图（原 book-to-skill 的 Mode 5）搬进 `book-to-judgment-navigation` 作为流水线第 2 步**。它本来就不读 PDF、只读冻结原子，不需要 book-to-skill 的提取器。**一条流水线从头跑到尾，一个技能搞定，不再跨技能串门**。`book-to-skill` 完全恢复原厂。 |
| **Q6** | **中央一次合成、单次交付**：主窗口在沙箱里把"拆网精简 ＋ 步骤哈希 ＋ 共享常量 ＋ 方案 C"一次做完并跑通全部单测，打成唯一升级包推给生产窗口覆盖。**生产窗口不参与任何代码改造讨论**，只认必读卡、只管生产。 |

### F 组：恢复生产

| 编号 | 定案 |
|---|---|
| **Q20** | **第一批继续蒸馏 BPHS 14~16 章（兄弟、住房、子女）**——老类型零件，一次只变一个变量，先把新工具跑顺。**ch74 决策树放第二批**做（新规格），绝不节外生枝。 |
| **Q22** | **跑通判据三条，全部机器对账**：①连续 3 批**零新病根家族** ②连续 3 批**100% 一轮通关**（无二次修改、无隔离）③**总账数字 100% 闭合**（合并脚本自动核对，无手工补账）。三条同时满足 → 宣布流程跑通 → 开 3~5 条并行线批量。 |

---

## 三、拆网四步（执行顺序）

```text
┌────────────────────────────────────────────────────────────┐
│ 步骤 1【物理清障】                                          │
│  ・bphs-97-book（870 万字、4,062 个作废 card_id）→ _已退役/ │
│  ・_backups（1,600 个同名旧版本）移出 .codex/skills/        │
│    ↑ 这两条在用户本机执行，主窗口无法代做                    │
│  ・两组重复黑名单抽成 shared_constants.py（已验证逐字一致）  │
└────────────────────────┬───────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────────┐
│ 步骤 2【流程降维】                                          │
│  ・开工必读锁死在《必读卡》（约 3 千字符）                   │
│  ・其余详规全部降为"备查字典"，只在报错时按指路表精准翻      │
│  ・防反弹铁律：发现漏了规矩，只准在指路表加一行链接，        │
│    严禁把长篇细则贴进卡里。谁贴谁违规。                      │
└────────────────────────┬───────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────────┐
│ 步骤 3【激活生门】                                          │
│  ・立即把《12 题盲测卷（修订版）》发给 RAG 窗口开考          │
│  ・与排盘器、蒸馏进度完全解耦，今天就能产生真实价值          │
└────────────────────────┬───────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────────┐
│ 步骤 4【恢复生产】                                          │
│  ・第一批：14~16 章零件（验新工具）                          │
│  ・第二批：ch74 宫位决策树（验单章清单新规格）               │
│  ・三条判据达标 → 开并行线                                   │
└────────────────────────────────────────────────────────────┘
```

---

## 四、要改的代码（三处，精确到位置）

| # | 文件 | 改什么 |
|---|---|---|
| 1 | `scripts/shared_constants.py`（新建，已完成） | 抽出 `EMPTY_CLAIM_FRAGMENTS` 与 `PLACEHOLDER_FACT_PATTERNS`；`build_methods.py` 与 `validate_delivery.py` 改为 import。搬家前实测两份逐字一致，**只搬不改** |
| 2 | `build_methods.py` `assemble_step()` ＋ `validate_delivery.py` `validate_status_receipts()` | 每步生成 `step_hash`；审计凭证从"绑整份 `method_recipes_sha`"改为"逐步骤绑 `step_hash`" |
| 3 | `build_methods.py` `validate_claim()` ＋ schema | 方案 C：`claim_terms` 增加四类高风险词强制映射（专名／数值／单位／时间词）；机器输出差异表进生成报告 |
| 4 | `templates/JUDGMENT_METHOD.json.template` ＋ `method-extraction.schema.json` | 加 `spine_slot_hint: null` |
| 5 | 新脚本 `backfill_step_hash.py` | 一次性回填已并入 635 条的 `step_hash`，纯计算不动内容 |

**基线**：搬家后实测 **127 项单测**（build 24 ＋ advance 7 ＋ merge 15 ＋ jh8 17 ＋ validate 64）。改完不许少于这个数。
两处失败只因硬编码 Windows 路径（`C:/Users/...` 试点夹具、`F:/JH8-.../01_命盘完整包.json`），在用户机器上会过，不是搬家搬坏。

---

## 五、交付清单（主窗口出，用户转，生产窗口装）

1. `book-to-judgment-navigation/` 完整升级包（含搬进来的 9 个 cangjie 文件 ＋ 知识地图第 2 步）
2. `cangjie-skill/`、`book-to-skill/` 恢复原厂说明
3. 《必读卡》（作为新技能 SKILL.md）
4. 逐句对账表：**旧文件哪一行 → 新文件哪一节**，一条对不上就是漏了
5. 回填脚本 ＋ 使用说明
6. 本定案文件

---

## 六、未决 / 留空（明确标出，防以后当成漏项）

- **PVR、K.N. Rao、300C 尚未加工**：第 1、3 层留空，`spine_slot_hint` 全部 null
- **`differences/` 700 万字符未检查**：100 章版可能有 97 版没有的措辞，本轮未查
- **第 45、46 章只读了片段**：体量最大（191KB / 96KB），若有隐藏方法型偈颂最可能在这两章
- **`_backups` 与 `bphs-97-book` 的物理移动**：在用户本机，主窗口无法代做
