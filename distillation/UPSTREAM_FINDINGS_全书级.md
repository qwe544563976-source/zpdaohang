# 全书级上游问题记录（蒸馏窗口只登记，不修源）

> 本文只登记蒸馏窗口在只读正式书包时发现、但**不属于当前批次范围**的上游问题。
> 批次范围内的问题写在各批次目录的 `UPSTREAM_ISSUES.json`（机器记录）。
> 蒸馏窗口不得修改原文原子、不得生成替代原子、不得把问题标记为已解决；
> 只能等切卡／OCR 窗口重新出包后从零重跑。

## P0-1：第 46 章 147~157 偈正文内容缺失

| 项目 | 内容 |
|---|---|
| 原文原子编号 | `bphs-97:santhanam:ch46:v147-157` |
| 章节 | 第 46 章（Dasha 体系，Vimshottari 相关） |
| 实际 `exact_text` | `(147-157) …` —— 只有偈号和省略号，正文整段缺失 |
| `content_missing` | `true`（上游已如实标记，未隐瞒） |
| 严重度 | **P0**：任何引用该原子的判断步骤都无法成立；缺的是大运章正文 |
| 当前影响 | 本批（第 14~16 章）不受影响；**将来做运期／大运专题时必然踩到** |
| 发现方式 | `distillation/setup_workspace.py` 的蒸馏窗口独立复算（全书 2024 条逐条扫 `content_missing`） |
| 需要谁处理 | OCR／切卡窗口：补齐该段正文后重新出包 |
| 蒸馏窗口的处置 | 不修、不猜、不用 book.md 补；做运期专题前该原子不得进入任何方法步骤 |

全书 2024 条中带 `content_missing=true` 的只有这 1 条，其余 2023 条正文完整；
`suspected_annotations` 全书为空。

## 复算结论（与上游权威验包结论并列，不互相替代）

| 检查项 | 结果 |
|---|---|
| `evidence_atoms.jsonl` 内容摘要 | 实测 `6ee0276dc0e8c1de94799326084ed5341f6d7b6a97089e3dd94e53ee5627f248`，与书包凭证 `content_sha256` 逐字一致 |
| 记录数 | 2024，与凭证 `record_count` 和状态总账一致 |
| 章节覆盖 | 第 1~97 章，97 个章号无缺口 |
| 编号唯一性 | 无重复 |
| 必备字段 | 2024 条全部齐全 |
| `previous_atom_id` / `next_atom_id` 链 | 无指向不存在原子的断链 |
| 上游权威验包结论 | 包内 `accepted-package-20260818T213650Z-validation.json`：`status=pass`、`errors=[]` |

**没有做的事（如实声明，不冒充）**：
- 没有运行上游权威验包器 `validate_accepted_package.py`——该脚本属切卡窗口，不在书包内，本环境无法调用；
- 没有打开原始 PDF 复核逐页结构和表格——属上游责任，蒸馏窗口只认上游凭证与真实退出码。

若要在本环境补跑真正的权威验包，需要把 `F:/切卡实验/aclass-chunker/validation/validate_accepted_package.py`
一并提供。
