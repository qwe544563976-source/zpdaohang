# 当前进度与续跑指引

更新：2026-08-21
一句话：**V2 升级已交付；BPHS 第一批（ch14~16）已过第一轮三视角审核并按审计结果修完硬错误（run03）；
第二轮确认审核在跑；批量前还有 2 条要老板拍板。**

## 一、现在在哪一步

| 阶段 | 状态 |
|---|---|
| V2 升级包 | 🟢 完成，168 单测全绿（基线 127） |
| 正式书包接入 | 🟢 2024 条原子，sha256 与凭证逐字闭合 |
| 全书批次规划 | 🟢 69 批，标签唯一，覆盖零遗漏（`BATCH_PLAN.json`） |
| 第一批抽取 | 🟢 四组并行，各自过生成器强校验 |
| 第一批装配 | 🟢 run03：46 方法 71 步，`--require-v2` 零错误 |
| 第一轮审核 | 🟢 73/73 全审，5 REJECT，硬错误全修（见 `审计有效性对照表.md`） |
| 第二轮确认审核 | 🔵 跑中（锁定被改动的 15 步） |
| 批量 2024 条 | 🟡 待第二轮通过 ＋ 2 条拍板 |

## 二、批量前要老板拍板的 2 条

已自行解决的两条见 `事实键命名规范.md`（B3）与 `待定事实清单.md`（B4）。剩下这两条要你定：

1. **"强弱"由谁算**（B1/B2）。原文多处说「有力」「估量强弱」，共 6 条事实卡在这上面，
   其中 `ch14-v15` 是死锁（唯一必查事实是强弱，同时禁止自定义算法）。
   我的建议：由排盘窗口交付六力（Shadbala）分数，方法只判是否达线。
   在你定之前，这类方法保持 `executable_in_pilot=false`。
2. **`ch14-v15` 怎么处置**。完整性审计判它应该是 `knowledge_only`（程序性告诫，不含条件→结果），
   我倾向采纳：把「先估强弱再宣布」写进 ch14 其他方法的适用范围，而不是单独做成一个空转方法。
   要动的话在 run04 一起做。

## 三、续跑命令

```bash
# 看第二轮审核结果
python3 -c "import json;d=json.load(open('/tmp/.../wd03372wy.output'));print(d['logs'])"

# 批量：导出下一批（计划里 69 批，第一批已做 ch14-16）
python3 distillation/export_batch.py --list          # 看全部批次
python3 distillation/export_batch.py ch12-13         # 导出某一批

# 抽取用 audit_workflow.js 同款方式起 workflow，产物落 _local/batches/<批次>/fragments/
# 然后装配：
python3 distillation/make_batch.py --batch-id <ID> --target distillation/bphs-97/batches/<ID>
```

## 四、这一路拿真实原文修在根上的九处（都有正反测试）

1. 方案 C 漏基数词（`three mothers`/`six Grahas`/`Nine sons` 检出 0）
2. 方案 C 漏译本短形式（`Putr`/`Shukr`/`Lagn`/`Mandi`/`Navāńś`）
3. 高风险词覆盖用子串（`100 sons` 假装覆盖 `10`）
4. 生成器强制每条原文都变方法（过渡句被迫做成假方法）
5. OR 闸门不分条件侧结果侧（`and/or sisters` 被误拦）
6. 析取藏进事实名（3 处真实违规）
7. 隐性合取「及其」（把四选一写成两者都要）
8. 结果动词加码（`pass away` → 「夭亡」，凭空补年龄）
9. 关键术语译名越界（`exalted`→「庙」、`trine`→「三分宫」）

第 9 项第一版写严了（要求同一词译名全批一致），拿真实批次一跑报了几十处假阳性，
改成只对「译错等于换一张盘」的词卡死禁用译名。**新闸门必须拿真实批次验。**

## 五、只能在老板本机做的

- 并入全书总账（`merge_method_batch.py` 需要含已并入 635 条的总账，不在本仓库）
- 回填 635 条 `step_hash`（`backfill_step_hash.py`）
- 装 V2 升级包、物理清障

## 六、如实声明的边界

- 上游权威验包器不在书包内，本环境无法真跑；开工闸门由「包内上游 pass 报告 ＋ 蒸馏窗口独立复算」并列支撑
- 全书级 P0 已登记：`ch46:v147-157` 正文缺失（大运章）
- 未修的结构性问题见 `待修清单_审计发现.md`（8 条可后补）
