# 当前进度与续跑指引

日期：2026-08-20（额度用尽时的断点）
一句话：**V2 升级已交付；BPHS 第一批（ch14~16，41 条原文）已产出 46 个方法 73 步并通过机械验收；
三视角独立审核因额度耗尽全部中断，这是唯一卡住的一步。**

## 续跑第一条命令

额度恢复后直接跑（args 里的 step_ids 从生成报告取）：

```bash
python3 - <<'PY'
import json
b="distillation/bphs-97/batches/BPHS97_ch14-16_siblings-home-children_20260820_run02"
r=json.load(open(f"{b}/validation/build-methods-report.json",encoding="utf-8"))
print(json.dumps({"target":f"/home/user/zpdaohang/{b}","step_ids":r["audit_step_ids"]},ensure_ascii=False))
PY
```

把输出作为 args，调用：
`Workflow({scriptPath:"/home/user/zpdaohang/distillation/audit_workflow.js", args:<上面的输出>})`

审核跑完后拿 `distillation/审计有效性对照表.md` 的两条种子问题对照，
**审计抓不到就先改审计提示词再重跑**，不许"反正主窗口发现了"就放过——
批量阶段主窗口不可能逐条看 2024 条原文。

## 已完成

| 阶段 | 状态 | 产物 |
|---|---|---|
| V2 升级包 | 🟢 完成 | `book-to-judgment-navigation/`，162 单测全绿（基线 127） |
| 正式书包接入 | 🟢 完成 | 2024 条原子，实测 sha256 与凭证逐字闭合，97 章无缺口 |
| 全书批次规划 | 🟢 完成 | `BATCH_PLAN.json`：69 批，标签唯一，覆盖 2024 条零遗漏 |
| 第一批抽取 | 🟢 完成 | 四组并行，各自通过生成器强校验 |
| 第一批装配 | 🟢 完成 | run02：46 方法 73 步，`--require-v2` 退出码 0 |
| 第一批审核 | 🔴 中断 | 6 个审核 agent 全部因 session limit 失败 |
| 批量 2024 条 | 🔴 未开工 | 必须审核通过后才开始（老板明确要求） |

## 拿真实原文跑出来的六处根源修复（都改在工具上，不是绕过）

1. 方案 C 漏基数词（`three mothers`、`six Grahas`、`Nine sons` 检出 0）→ 词表加 two~twelve
2. 方案 C 漏译本短形式（`Putr`/`Shukr`/`Lagn`/`Mandi`/`Navāńś`）→ 补专名与分盘名
3. 高风险词覆盖用子串（`100 sons` 假装覆盖 `10`）→ 改按词边界
4. 生成器强制每条原文都变方法（过渡句被迫做成假方法，97 章会累积上百条）→ 支持 `knowledge_only` 去向
5. OR 闸门不分条件侧结果侧（`and/or sisters` 被误拦，绕过它只能截断短引犯另一个错误家族）→ 保留 `claim_terms` 后按结果映射区分
6. 析取藏进事实名（3 处真实违规）→ 生成器硬拦，并规范化「基座事实」写法

## 第一批已修的三处内容问题

- 3 处析取事实名 → 原子基座事实（分支一个未减）
- `ch16 v13` 的 `Decanate` 译名：十分盘（D10）→ 三分盘（D3），写错等于换一张盘
- `ch15 v1` 纯过渡句 → `knowledge_only`，不再做成假方法

## 只能在老板本机做的

- 并入全书总账：`merge_method_batch.py` 需要含已并入 635 条的总账，不在本仓库
- 回填 635 条 `step_hash`：`backfill_step_hash.py`
- 装 V2 升级包、物理清障

## 边界（没做的事，如实写）

- 上游权威验包器 `validate_accepted_package.py` 不在书包内，本环境无法真跑；
  开工闸门由「包内上游 pass 报告 ＋ 蒸馏窗口独立复算」并列支撑
- 全书级 P0 已登记：`ch46:v147-157` 正文缺失（大运章，做运期专题前必踩）
