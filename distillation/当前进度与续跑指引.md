# 当前进度与续跑指引

更新：2026-08-21
一句话：**V2 升级已交付；第 1 批（ch14~16）两轮审核通过并按审计结果修完（run04）；
第 2 批（ch17~18）已装配过机检、审核跑中；流水线已改成不绑批次，可连着往下推。**

## 一、现在在哪一步

| 阶段 | 状态 |
|---|---|
| V2 升级包 | 🟢 完成，168 单测全绿（基线 127） |
| 正式书包接入 | 🟢 2024 条原子，sha256 与凭证逐字闭合 |
| 全书批次规划 | 🟢 69 批，标签唯一，覆盖零遗漏（`BATCH_PLAN.json`） |
| 第 1 批 ch14-16 | 🟢 run04 交付：46 方法 71 步，两轮审核通过 |
| 第 2 批 ch17-18 | 🟢 run01 装配：77 方法 94 步，`--require-v2` 零错误零警告；🔵 第一轮审核跑中 |
| 第 3 批 ch19-20 | 🟡 分组已导出（24 条 / 2 组），等第 2 批审核结论再起抽取 |
| 剩余 66 批 | ⚪ 未开工 |

## 二、每批的固定五步

```bash
L=ch19-20                      # 批次标签，见 BATCH_PLAN.json
ID=BPHS97_${L}_<主题slug>_$(date +%Y%m%d)_run01

# 1 导出分组（章名自动取自原子，不要手写）
python3 distillation/export_batch.py $L

# 2 抽取：起 workflow，每组一个 agent，产物落
#   distillation/_local/batches/$L/fragments/g*.json
#   抽取员每产一组就自验：python3 distillation/check_fragment.py <片段> 直到 passed
#   没有判断规则的原文写进 distillation/_local/batches/$L/knowledge_only.json

# 3 装配 + 机检
python3 distillation/make_batch.py --batch-id $ID \
  --target distillation/bphs-97/batches/$ID \
  --scope distillation/_local/batches/$L/scope.json \
  --fragments distillation/_local/batches/$L/fragments

# 4 第一轮审核（三视角，全批步骤逐字对照原文）
#    Workflow scriptPath=distillation/audit_workflow.js
#    args={target, batch:"$L", round:1, shards:N, step_ids:[...]}

# 5 按审计结果改 fragments → 重装配成 run02 → 第二轮审核（round:2，只锁被改动的步骤）
```

**两轮审核不是形式。** 第 1 批第二轮抓出的正是主窗口采纳错误建议后改坏的那一步，
详见 `审计有效性对照表.md` 第三节。

## 三、老板已拍板"先不管"的两条（批量期间保持挂账）

1. **方法之间不联网**（B1）：46 个方法 `dependencies` 全空，原文明写"先估强弱再宣布结果"
   是前置闸门。批量期间不接线，全书蒸馏完统一接。
2. **"强弱"由谁算**（B2）：原文多处说「有力」「估量强弱」。建议由排盘窗口交付六力
   （Shadbala）分数、方法只判是否达线；定案前这类方法保持 `executable_in_pilot=false`。

## 四、这一路拿真实原文修在根上的十二处（都有正反测试）

抽取与闸门（1~9 见第 1 批）：

1. 方案 C 漏基数词（`three mothers`/`six Grahas`/`Nine sons` 检出 0）
2. 方案 C 漏译本短形式（`Putr`/`Shukr`/`Lagn`/`Mandi`/`Navāńś`）
3. 高风险词覆盖用子串（`100 sons` 假装覆盖 `10`）
4. 生成器强制每条原文都变方法（过渡句被迫做成假方法）
5. OR 闸门不分条件侧结果侧（`and/or sisters` 被误拦）
6. 析取藏进事实名（3 处真实违规）
7. 隐性合取「及其」（把四选一写成两者都要）
8. 结果动词加码（`pass away` → 「夭亡」，凭空补年龄）
9. 关键术语译名越界（`exalted`→「庙」、`trine`→「三分宫」）

流水线不绑批次（10~12，第 2 批开工时暴露）：

10. **批次主题手写章名会错位**：我按目录范围手写成 ch17=Yuvati 七宫，实际 ch17 是 Ari 六宫。
    四个抽取组里三个独立发现并按原子的 `chapter_title` 走。已改为一律从原子派生，
    并加 `bhava_house_number()` 反向核对——拿 ch17 冒充 Yuvati 会当场停工。
11. **knowledge_only 判定没有合法写入口**：抽取员只能去改全局共享清单，四组并行会抢文件。
    已改成批次目录写、装配时提升进全局永久档（批次目录在 `_local/` 下不入库）。
12. **知识地图主题与术语写死 ch14~16**：第 2 批装配当场死在"没有可建主题或术语的原文"。
    已改为全部从 `chapter_title` 派生，全 97 章实测 slug 无撞车；
    宫位序号出自原文 `ch07:v37-38` 自列的十二宫名次序，每次装配回原文核一遍。

第 9 项第一版写严了（要求同一词译名全批一致），拿真实批次一跑报了几十处假阳性，
改成只对「译错等于换一张盘」的词卡死禁用译名。**新闸门必须拿真实批次验。**

## 五、全书级挂账

- `ch46:v147-157` 正文缺失（原文只有 `(147-157) …`）。做到大运章节会撞上，
  已记入 `UPSTREAM_FINDINGS_全书级.md`。
- 术语解释不写星占含义：知识地图契约写明术语解释"只是导航辅助，永远不是证据"。
  唯一带含义的是宫位序号，因为它有原文出处。
