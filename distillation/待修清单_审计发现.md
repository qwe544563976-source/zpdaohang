# 审计发现的待修项（run03 之后仍未解决）

第一轮三视角审计对 run02 的 73 步全审，5 个 REJECT、实用性 NEEDS_WORK、完整性 GAPS_FOUND。
**run03 已修完全部有明确原文依据的硬错误**（见该 commit）；本文只记还没修的结构性问题，
不冒充已完成。这些问题不影响单条方法的原文忠实度，影响的是"方法之间能不能联网工作"。

## 老板已拍板：B1／B2 延后，先批量

2026-08-21 老板决定：**依赖接线（B1）与「强弱由谁算」（B2）先不管，直接开始批量。**

这意味着接受以下已知后果，批量结束后统一返工：

- 46（未来约 2000）个方法的 `dependencies` 保持 `[]`，方法之间不联网；
- 同一触发条件可能命中互相矛盾的结论（如"五宫主落六/八/十二宫"同时命中「无子嗣」与「得子艰难」），
  两者的反例路互不指向对方，仲裁要等接线后再做；
- 原文明写的前置闸门（"先估强弱再宣布结果"）暂不生效；
- `ch14-v15` 这类死锁方法保持 `executable_in_pilot=false`，不进执行主路。

**返工成本预估**：接线是给方法加 `dependencies` 与交叉 `counter_queries`，属结构性字段，
不需要重读原文、不需要重新抽取，可由脚本按事实键与主题批量生成候选、再由审计确认。
比"重抽 2000 个方法"低一个量级——这是这个决定成立的前提。

**批量期间的纪律**：抽取员照常按原文写方法，不要因为没接线就自己发明依赖关系。
遇到原文明写"先做 X 再做 Y"的，写进 `applicability_scope` 备注，留待接线时用。

## 一、必须在批量前定的（老板已决定延后，保留原文备查）

| # | 问题 | 为什么必须先定 | 建议 |
|---|---|---|---|
| B1 | **依赖全空**：46 个方法 `dependencies` 全 `[]`，但原文明写"先估强弱再宣布结果"（ch14 v4½、v15）是前置闸门 | 这是书自己规定的程序。批量后再补要回头改 2000+ 方法 | 先决定"强弱"由哪个方法或外部引擎供给，再统一接线 |
| B2 | **ch14-v15 死锁**：唯一必查事实是"三宫相关行星与瑜伽组合的强弱"，`forbidden_extensions` 又禁止自定义强弱算法 → 该事实永远取不到值 | 闸门要么不接线（等于空转），要么一接线就锁死整个 ch14 | 与 B1 一并定：强弱是 Shadbala（六力）还是别的，由谁算 |
| B3 | **事实键未归一**：182 个事实键只有 9 个被两个以上方法复用，大量同义异名（"本盘五宫主是哪颗行星" vs "五宫主（Putr's Lord）是哪一颗行星"） | 事实键是交给排盘的接口，不归一等于同一件事要算很多遍，且 JH8 对接时要写一堆别名映射 | 出一份事实键命名规范＋别名表，批量前定 |
| B4 | **必查事实排盘算不出来且未登记上游问题**：如 ch15-v6-v7 的"母亲的指示星是否有力"——指示星是谁本批未定（原文该句没像别处那样给括号指代） | 接线后即永久停判，而且一条 `UPSTREAM_ISSUES` 都没登记 | 建立"事实可算性"检查：写进 required_facts 前先确认排盘能给 |

## 二、本批可以后补的（不阻塞批量）

| # | 问题 | 说明 |
|---|---|---|
| B5 | `ch16-v24-32-number-of-sons` 粒度：5 条互斥备选（十子/九子/八子/七子双生/七子三亡）塞进一个方法，方法级 `stop_conditions` 合取 → 能判十子分支的盘，只要缺九子分支的事实照样整方法停判 | 需拆成 5 个独立方法，重抽 |
| B6 | 同触发条件矛盾结论无仲裁：五宫主落六/八/十二宫同时触发"无子嗣"与"得子艰难"，两者 `counter_queries` 互不指向 | 需要跨方法交叉链接机制 |
| B7 | 边界路系统性模板化：92 条 `boundary_queries` 有 40 条以"……的适用边界"结尾，对古典原文零可检索性 | 批量前改抽取提示词，给正确样板 |
| B8 | 反例路约三分之一在自造教条而非找反例（如"benefic drishti cancels affliction"这类原文没有的规则） | 同上 |
| B9 | `context_reference` 全批 0 次使用：ch14-v3 的"前述两星"承接只写在散文里，`evidence_refs` 没绑 ch14:v2 | 承接关系应有机器绑定 |
| B10 | 男命框架标注不一致：ch16-v5/v6 标了"原文以男命之妻立说"，同样男命立说的 ch16-v4、ch16-v24-32 没标 | 女命用户命中时无提示 |
| B11 | `ch15-v3` 章题错位（原文是 Putr's Lord 却排在第 15 章）处理正确但没在 `UPSTREAM_ISSUES` 登记 | 应登记为原文自带特征，让下游 RAG 知道不是抽取失误 |
| B12 | `ch16-v24-32` 通篇只说 sons（儿子），`user_intents` 却挂到"子女"总数问题上；书本身区分 son 与 child（v5/v6 说的是 one child） | 路由意图放大 |

## 三、机器闸门盲区（可机器化，建议批量前补）

- **译名一致性**：同一英文词在批内被译成两个中文词（`exalted` 8:3、`trine` 37:1）——可批内建译名表，冲突即告警。这次两处都是靠人审抓到的。
- **隐性合取藏进事实名**：现有析取闸门只认"或/之一/任一"，抓不到"及其"（"四宫**及其**宫主"实为四选一压成 AND）。
- **结果动词中文加码**：`pass away → 夭亡`、`lose → 夭折` 凭空补上年龄限定。四类高风险词只覆盖专名/数值/单位/时间，不覆盖结果动词。

---

## 第 2 批（ch17-18）第一轮审计之后新增（2026-08-21）

第一轮：94 步全审，**2 REJECT**（第 1 批同口径是 73 步 5 REJECT），
实用性 NEEDS_WORK、完整性 GAPS_FOUND（但 `missing_rules` 为空——规则覆盖本身没有漏）。
两处 REJECT 与四条续查线索已在 run02 修完，以下是**还没修的**。

### C1｜（已解决 2026-08-21）第 1 批 run04 带着"装配丢字段"缺陷

生成器补回 `context_bindings`／`context_reference`／`time_scope` 之前产出的批次，
交付件里这三个字段都是空的。第 1 批 run04 抽取端 71 步写得正确、交付件 grep 计数为 0。

- **要做**：用修好的生成器把第 1 批重装配成 run05。
- **代价**：`step_hash` 覆盖步骤全部字段，加字段＝71 步指纹全变，run03／run04 两轮审计凭证全部失效。
- **省钱的做法**（不是全量重审）：run04 与 run05 两个目录都在盘上，
  审计员可以逐步骤 diff，先确认**只有这三个字段不同**，再单独核对这三个字段是否与原文一致。
  这比重读原文全审便宜得多，且同样严谨。
- **不做的后果**：第 1 批交付件里 34 处跨偈承接关系缺失，只读交付件的下游会看到
  "凭空多出来的条件"；`time_scope` 缺失让"第26岁得痨病"这类断语的年份没有可读字段。

### C2｜罗睺／计都同句共用一个结果，本批内前后拆法不一致

- `ch17-v3-graha-limb-map`：原文「and the Rahu and Ketu of the abdomen」拆成 step-008／step-009 两步；
- `ch18-v7-8-5-shani-rahu-ketu-base-females`：原文「denoted by Shani, Rahu/Ketu in Yuvati」
  做成一个步骤内的 OR 分支，没拆。

两种写法各自都忠实原文（完整性审计明说风险不大），问题只在**同一批用了相反口径**。
建议统一为**按行星拆**：用户盘里有罗睺没有"罗睺/计都"，拆开每一支才能独立求值。
未做的原因：改法会动步骤结构，等第二轮审计结论回来再一起改，避免改完又要再审一轮。

### C3｜主题层是方法的一比一影子，没有聚合

`query-recipes.json` 77 个 topic 对 77 个方法，`candidate_method_ids` 长度 >1 的是 0 个。
结果：8 个 topic 挂着一模一样的 user_intent「我大概几岁结婚」、4 个挂「我会不会得麻风一类的重皮肤病」。
意图匹配没法排序也没法收敛。婚期、麻风、配偶体貌、失妻定时这几族各该出一个汇总 topic。

这是**生成器层面的结构**问题（topic 由 `build_methods.py` 按方法一一生成），
不是抽取问题，改动面比单批大，留到全书批量结束后统一做。

### C4｜反例路在 ch17 塌成四句通用消灾语

ch17 的 27 个方法里 19 个，英文反例词只由四句通用消灾语组成
（"Should one among Budh, Guru and Shukra be in an angle from Lagna, all evils are destroyed" 出现 16 次等）。
问麻风、问天花、问第45岁血麻风，反例路取回同一批文档，等于没有反例路。
同批 `ch17-v2`／`ch17-v3` 用的 "The limb, related to a benefic, will have a mark (like moles etc)"
才是真正贴题的反例。属改进项不是阻塞项，已写进抽取规则供后续批次注意。

### C5｜两处上游原文异常没有登记

`UPSTREAM_ISSUES.json` 是空的，但本批实测到两处：
- `ch18:v26` 的 "The native will marry at 12, or l9" 里 `l9` 明显是 19 的排印／OCR 讹字
  （方法已正确保留原样并标"原文如此印"，但没回上游登记）；
- `ch18:v7-8.5` 与 `ch18:v8-9.5` 两条原子的偈号区间在第 8 偈上重叠，
  日后引用「BPHS 18.8」会指向两条原子。

两件都该进 `UPSTREAM_FINDINGS_全书级.md`，已补。


---

## 2026-08-21 晚间结算

### C1 已解决
第 1 批重装配成 run05：原先丢掉的 12 步承接关系、10 步时间限定、1 步让步条件都进了交付件。
根因也补上了——`STEP_FIELD_DESTINATIONS` 登记抽取端每个字段的去向，
装配时逐步骤核对，漏抄或新增未登记字段都当场停工（正反样本验过）。

### C2 已解决（罗睺／计都拆法不一致）
第 2 批 run03 重装配时一并处理。

### 新增 C6｜四批欠一轮复审
第 1 批 run05、第 2 批 run03、第 4 批 run02、第 5 批 run02 都是重装配后的新版本，
步骤指纹全变，旧审计凭证作废。内容改动本身有明确原文依据（见各自 commit），
但按"反假绿"纪律，**未经复审不得当作已验收**。

### 新增 C7｜own Rāśi 被写成"本宫"，两轮审核都没抓到
`ch16:v1-3`、`ch18:v1`、`ch18:v22` 三处，全在已过两轮审核的批次里。
是新加的短语级机器闸门抓到的，不是审计抓到的。
**结论：机器闸门与人工审计各有盲区，不能只靠其中一个。**
译名类错误优先做成机器闸门——它便宜、稳定、可回归；
审计的价值在机器表达不了的地方（有没有多说、承接对不对、原文有没有授权）。

---

## 全书跑完统一修（老板 2026-08-21 19:24 定：不准边修边跑）

从此刻起批量阶段只做两件事：抽取＋装配落袋。以下情况一律**登记不修**，
全书 69 批抽完后统一修：

- 装配机检未过的批次：登记报错原样，目录不保留，片段留在 _local 工作区；
- 抽取 agent 自验没过、反复卡住的组：登记卡点，跳过该组继续；
- 所有语义审计（每批三视角、两轮）：全书跑完后统一安排节奏；
- 已知欠账 C6（四批复审）、C8（第1、2批被剔除的 317 条 V2 宽词车道清洗）照旧挂账。

### 统一修清单（滚动登记）
| 批次 | 问题 | 登记时间 |
|---|---|---|
| （ch36、ch04-05 在该指令下达前已修完落袋） | | |
| ch06-07 | 装配机检：方法 ch06-v24-26-saptavimsamsa-division 声明的必查事实「待求分盘的黄经在所在星座（Rāśi）内的度数是多少」没有被任何步骤使用。片段在 _local/batches/ch06-07/fragments/，仅此一处，其余 47 方法机检项均过 | 2026-08-22 |
